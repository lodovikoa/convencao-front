import { RecuperarSenhaDTO } from './recuperar-senha-dto';

describe('RecuperarSenhaDTO', () => {
  it('should create an instance', () => {
    expect(new RecuperarSenhaDTO()).toBeTruthy();
  });
});
